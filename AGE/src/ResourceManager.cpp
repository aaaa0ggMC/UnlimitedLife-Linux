//#include <foonathan/memory/container.hpp>
//#include <foonathan/memory/memory_pool.hpp>
/*#include <string.h>


#include <AGE/ResourceManager.h>

using namespace age;
//using namespace memory::literals;

ResourceManager::ResourceManager(unsigned int poolSize){
  //  mempool = new MemPool();
}

ResourceManager::~ResourceManager(){
    //if(mempool){
      //  delete mempool;
    //}
}

std::optional<std::string *> allocateString(const std::string& buf){
    auto result = stringbufs.find(buf);
    if(result == stringbufs.end()){
        std::string * buf = new (mempool) string;
        if(!buf)return std::nullopt;
        (*buf)
        stringbufs.emplace(*buf,buf);
        return {buf};
    }
    return {result->second};

}*/
